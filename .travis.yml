dist: xenial   # required for Python >= 3.7
language: python
python:
  - 3.6
  - 3.7
os:
  - linux
git:
  depth: 1
before_install:
  - pip install pip --upgrade
  - pip install -r requirements.dev.txt
install:
  - make certs
  - make generate
  - make style.check
  - pip install .[amq,protobuf,msgpack,avro,brotli,yaml]
  - make dist
script:
  - make test
  - make docs
